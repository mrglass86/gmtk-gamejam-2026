# Adversarial QA findings

Last run: 2026-07-24 — production-scene QA harness plus normal-root restart
audit and state-collision battery, Godot 4.7.1-stable.

| Severity | Title | Repro steps | Expected vs actual | Suspected file/cause | Suggested owner |
|---|---|---|---|---|---|
| major — persisting | Win zone extends well beyond the physical crib | Start a run; put the snack and player at `Crib + (1.95, 0.6, 0)`; collect it; advance three physics frames. | Expected: being beside the crib should not win. Actual: the player centre is 2.04 m from the crib centre and immediately wins. The physical crib rails reach only about 1.09 m on X. | `game/scripts/GameFlow.gd`: `_is_player_in_crib()` fallback accepts `BoxShape3D.size / 2 + goal_body_margin` (1.6 + 0.4 m) instead of requiring the Area overlap. | A |
| major — fixed | Caught while carrying snack could have been a free win | Start a live run; reveal and collect the snack at the catch point; invoke the production parent catch path; advance 180 physics frames. | Expected: snack drops at the catch point, player stays empty-handed, and GameFlow remains PLAYING. Actual: exactly that; no free win reproduced. | Fixed in `game/scripts/Parent.gd` catch/drop ordering and `game/scripts/Snack.gd` pickup lockout. | B — regression coverage only |
| minor — closed (harness-only) | Headless live-scene shutdown reports active OGG resources | Lane C still reproduces warnings when it frees a playing child scene and quits two frames later. The normal-root audit started five players, called `GameFlow._request_restart()`, and then quit after the audio server's normal 0.75 s settle. | Expected: restart frees the old scene audio. Actual: the old scene, old `AudioDirector`, and all 19 old player weak references became invalid; the new scene had exactly one director with 19 fresh players; normal quit emitted no ObjectDB/resource markers. | Lane C's immediate mid-audio child-scene teardown exits before the audio server settles. No production ownership leak and no code fix required. | Closed |
| polish — persisting | Full-scene restart reload remains outside the child-scene harness | The restart battery delivers restart during the `game_ended` signal, then 39 more presses, for both LOSS and WIN. It uses `reload_scene_on_restart = false` so the harness can observe all 40 requests. | Expected: one normal-root scene reload should replace this test seam. Actual: all 40 production restart-handler calls are coherent and leave the terminal state stable when reload is intentionally suppressed; the normal-root audit separately confirms five actual reloads clean up their old audio players. | Test topology rather than a confirmed gameplay defect. | A — only if expanding QA to a root-scene stress runner |
| polish — persisting | State-collision battery found no duplicate terminal result or snack soft-lock | Run `expiry` once each for CARRY, FOUND, INVESTIGATE, active searchlight, and all ten epilogue substates; run restart fuzz; drop snack on a wall, switch, off-navmesh, and crib; then run peek/double-catch/pet-investigate collision. | Expected: one terminal loss per expiry; snack alone in crib does not win; nasty placements recollect; catch transitions remain stable. Actual: all 15 expiry cases emitted exactly one LOST, placements recollected and passive crib did not win, and collision cases stayed coherent. | No concrete gameplay cause found. | A/B — rerun after state-machine changes |
| polish — persisting | No gameplay exploit or soft-lock reproduced by the initial adversarial battery | Run `game/tests/qa/run_qa.sh`: caught-snack; 61 live switch events; then 5,200-frame monkey runs with seeded randomized movement, run, and interact inputs. | Expected: terminal win/loss, no impossible snack ownership, and no non-carry input lock beyond 22 seconds. Actual: all initial runs reached a terminal state (LOSS) and met the assertions. | No concrete gameplay cause found. | A/B — rerun after state-machine changes |
