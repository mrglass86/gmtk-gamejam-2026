# Lane A A8 handoff

## Completed

A8 landed across `33b7832`–`ed2c253`. TV/speaker masks are capped at 0.6 with
3.2 m/2.4 m radii; matching audio beds use the same reach. The snack is 0.35 m
radius, cream emissive, gently pulsing while revealed and carried, uses a
louder pickup cue, and triggers a mesh-only 0.3 s player scale-pop. Pantry
placement clears the wide panel at the gameplay camera angle.

## Current state

`--verify-a8`, A4, A7, audio, A6, B6, clean startup, and release Web export
pass on the integrated B7+A8 commit. A Compatibility-renderer capture confirms
the pantry snack is plainly visible.

## Decision

Masking muffles player noise but preserves at least 40% source loudness.
Presentation observes existing gameplay signals; actor collision is unchanged.

## Exact next action

Director replays CP4. Accept when TV-side player rings shrink but remain
visible, parent hearing still reacts, pantry pickup reads instantly, and the
pulsing carried snack stays obvious on the return leg.

## Risks

Final mask/music balance and pickup sting loudness remain feel calls.

## Changed files

`Main.tscn`, `LevelBuilder.gd`, `AudioDirector.gd`, `Main.gd`,
`SnackVisualPresenter.gd`, shared state/backlog, handoff.
