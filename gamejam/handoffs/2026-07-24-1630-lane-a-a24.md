# 2026-07-24 16:30 — lane A A24

## Completed

- Removed switch-originated `NoiseSystem` emission and the obsolete loudness
  export. The soft positional click remains.
- Widened all three toy hazards to 2.6 × 1.4 m and rebuilt each as a spread
  pill/train/block pile over one 0.02 m walkable overlay collider.

## Current state

- `--verify-a24` observes an active click player and zero switch gameplay
  events; three toy piles satisfy footprint, silhouette, surface, and collider
  checks.
- Editor, startup, and all 39 flags pass (41/41 functional gates); nav remains
  164. B14's known harness-only teardown warning is unchanged.
- Lane-C switch-spam (61 flips) and monkey seeds 260724/7331/9001 pass.

## Decision

Switch anomalies are visual-only. Toy coverage widens without per-piece
collision or new mechanics.

## Next action

Director walks through each pile and flips a switch near the parent. Accept
when the toys read as piles and switch use never creates a hearing alert.

## Risks

Only presentation scale remains subjective. A24 stacks on A22; A23 remains a
separate branch for integration.

## Files

`WorldSwitch.gd`, `NoiseSurface.gd`, `Main.tscn`, `Main.gd`.
