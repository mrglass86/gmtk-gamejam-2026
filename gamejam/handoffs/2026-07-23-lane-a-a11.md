# Lane A A11 handoff — 2026-07-23

## Completed

`6df8cff`/`6b48552` land A11. Positional shadow filtering is High; six Omnis
use blur 2.0 and all seven shadowed lights use opacity 0.8. Static sources sit
at y 4.5 toward room centers while emissive shades stay on furniture/floor
fixtures. The dog lies along local `-Z` with forward low snout and two ears.
Dining/kitchen tables have tops, four legs, four/three chairs, and exactly one
group collider each. The front-door floor lamp became a wall-side table lamp.
Each creak is three subtle export-tunable hardwood-family planks.

## Verification

Clean detached `6b48552`: A11/A10/A9/A4.1/A7/A8/audio/B9/B10 pass. Combined
B11 head `23ffd7c`: editor/startup plus A11/B6/B9/B10 pass. Navmesh is
connected at 157 polygons. Compatibility capture:
`2026-07-23-a11-lighting.png`.

## Next

Noah runs the CP4/CP5 director route. Accept when shadows have no hard black
wedges, dog direction and tables read at gameplay scale, the front-door path
is clear, creaks are discoverable but quiet, and S7–S10 remain playable.

Risk: source elevation narrows rendered floor pools; analytic brightness is
intentionally unchanged.

## Changed

`project.godot`, `Main.tscn`, `LevelBuilder.gd`, `NoiseSurface.gd`,
`DebugTools.gd`, `Main.gd`, shared memory, capture.
