# CP1 review — A0/A1/B1 (Claude, validator)

Verdict: A1 pass (S2, runtime spot-check at CP1). B1 pass on read (S3 runtime
at CP1). A0 structure approved with the fixes below. A2/A3 stay the critical
path — fold these fixes in alongside them.

## A0.1 fixes, ordered

1. **Navmesh (blocking for B3, do before parent behaviour lands).** The
   hand-authored polygons disagree with the collision geometry: the connector at
   x[-7,-6] z[-1,0] tunnels through the solid bedroom/dining wall, and the
   kitchen polygon covers `KitchenCounterWall` and the props — parent and pet
   would path through furniture. Replace with one startup bake of the built
   static colliders: LevelBuilder tags floors/walls/props into a `nav_source`
   group; `NavigationMesh` set to parse static colliders with source geometry =
   explicit group `nav_source`; `agent_radius` 0.4, `agent_max_climb` 0.25 (it
   must clear the 0.12-high hazard overlay boards); call
   `bake_navigation_mesh()` once at ready after the build and assert the result
   has polygons. This is still bake-once-static in spirit (risk check section
   2): geometry never changes after build; nothing rebakes during play.
2. **Lighting wash (blocking for CP1 readability).** `Sun` at 0.7 plus ambient
   0.45 flattens the lamp pools — the whole floor reads uniformly lit, which
   kills the value signal (brief 6.4) and the capsule readout contrast (S3).
   Drop `Sun.light_energy` to ~0.15 and `ambient_light_energy` to ~0.12 (both
   stay as faint fill), confirm the four lamp pools read as pools with real
   darkness between them, and re-capture the layout PNG for the director.
3. **BedroomDoor stub** sits at (-7, 0, 0), mid-wall — the two-door leftover.
   The design is one bedroom exit with the routes splitting outside it (brief
   6.8; your teacher-board placement already honors this). Move the stub to the
   actual doorway, ≈ (-7.35, 0, 3.9).
4. **Clear the WIRING item:** attach `res://scripts/Player.gd` to `Player`. The
   `Capsule` child and collision already exist; LevelBuilder already sets the
   floor groups — so this is attach-and-done.
5. Minor, defer-friendly: the door-strip glow belongs to the **parent's** door
   on the hallway (brief 7.1 "passes the parent's door"), not the kid's door.
   Next lighting touch, move it mid-hall on the south wall, ≈ (3, 0.05, 8.5).

## Flags — no action today

- Route lengths measure near 1:1 (the hall may even be shorter). Target: hall ≈
  1.4–1.6× dining at sneak speed (S10, measured Friday F3). Expect a geometry
  iteration; levers are the kitchen entrances and hall length.
- AreaLight3D glow orientations are visually unverified — check at the web
  export smoke (S11).
- A2 note: `VALID_ZONES` excludes the dynamic `fridge` light — the pre-approved
  helper is its own registry entry, not a zone member; do not route it through
  zone validation.

## CP1 run (after fixes 1, 2, 4)

Noah opens the project and presses play: walk all four surfaces, cross the
teacher board (no stutter stepping onto the 0.12 overlay), stand under a lamp
versus in dining shadow and watch the capsule change. Pass = VALIDATION S1 + S2
+ S3 rows.
