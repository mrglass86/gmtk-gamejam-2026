# Lane A handoff — A6

## Completed

- Added `DinnerGameFlow` title/play/win/loss state machine.
- First title input starts the Web-safe audio cue and `GameClock` together.
- Added non-blocking crib goal: holding snack there wins immediately; expiry
  wins only there with snack, otherwise loses.
- R requests `reload_current_scene()`. Added title/result cards and capture.
- Exported release Web build with the existing preset unchanged.

## Current state

- Commit: `d3a9d5d`.
- `--verify-a1` through `--verify-a6` and 600-frame smoke pass.
- Safari loaded/rendered the export and fetched HTML/JS/WASM/PCK/audio workers
  with HTTP 200. Chrome was installed but not running, so policy blocked launch
  without director permission; Safari focus changed before console/start proof.

## Decision

GameFlow is the sole owner of first-input clock/audio start and terminal states.

## Exact next action

With director permission, launch Chrome, load `http://127.0.0.1:8060/index.html`,
press one key, and accept only with a clean console and live play (S11).

## Risks

B5 is committed but its `Main.tscn` routine override still needs Lane A removal.

## Changed files

`GameFlow.gd`, `Main.gd`, `Main.tscn`, A6 title PNG, shared memory.
