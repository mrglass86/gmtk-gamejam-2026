# Handoff — 2026-07-23 13:31 — lane B

## Completed

- `946cd11` adds the reviewed B3.1 FOUND chase, defensive grab-distance gate,
  five-second LOS escape, and `GameClock.run_length` routine synchronization.
- B4 adds `DinnerPet`: clock-indexed patrol, one-second noise alert, navigation
  investigation, repeat suppression, and one-shot bark noise.

## Current state

- Godot 4.7.1 class scan passes. The A0.2 project starts normally with its
  155-polygon navigation bake. Pet scene wiring remains open.

## Decisions made

- Maximum suspicion now starts a readable chase before carry when the toddler
  is outside grab range. Recorded in `../DECISIONS.md`.

## Next action

- Lane A replaces Pet's `AgentStub.gd` with `res://scripts/Pet.gd` in
  `Main.tscn`, then runs S8. Acceptance: nearby noise produces one-second alert,
  investigation, at most one bark per eight seconds, and clock-current patrol
  resumption.

## Risks / unanswered questions

- Final patrol path feel needs CP5 playtesting. Alert and bark signals are ready
  for the later audio pass; no sound asset is assigned yet.

## Files changed

- `game/scripts/Parent.gd`, `game/scripts/Pet.gd`, `gamejam/WIRING.md`,
  `gamejam/DECISIONS.md`, this handoff.
