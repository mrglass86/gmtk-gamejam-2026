# Lane A handoff — A20 snack identity

## Completed

- `8f92e36` makes `SnackVisualPresenter` consume `snack_type`: chips render as
  a pulsing foil packet; ice cream renders as a pulsing cone/scoop.
- Pantry pickup/carry uses the current grab and wrapper pools. Fridge pickup
  uses the cleaned kid “mmm” pool plus a soft scoop and carries with a quiet
  spoon-tap skin; no fridge crinkle routes.
- Player remains sole gameplay-noise authority at loudness 0.3 every 0.6 s.
- B17 WIRING is cleared.

## Current state

- Exact committed editor/startup plus all 32 flags pass: 34/34, clean markers.
- `--verify-a20` covers both swaps, pulse, typed audio, no fridge crinkle, and
  unchanged mechanics. Nav remains 164.

## Decision

- Snack type changes presentation and audible flavor only, never stealth cost.

## Next action

- Noah runs CP5 through pantry and fridge goals, both result states,
  and restart. Accept when each snack reads immediately in motion and the
  fridge skin is distinct without weakening its return-leg risk.

## Risk

- Fridge “mmm” is a single take; scoop/tap are quiet pitched CC0 snack SFX.

## Changed files

- Four game scripts; `STATE.md`, `DECISIONS.md`, `BACKLOG.md`, `WIRING.md`.
