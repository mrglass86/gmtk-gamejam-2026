# 2026-07-24 15:57 — lane A A23 audio casting

## Completed

- Reserved `carry_red_handed_01` for the one-shot post-title motivation line.
- Recast catch to sting + `caught_grunt_03`; 01/02 now form the chance-based
  organic-reaction pool.
- Added state-lifetime chains: combined chase/win giggles during `FOUND`, and
  context-selected kid protests plus parent grunts during `CARRY`. Deposit or
  state exit stops them.
- Kept one priority-disciplined VO player and all existing CC0 fallbacks.

## Current state

- Editor, startup, `--verify-audio`, A6, B7/B8, B13–B16, and B20 pass.
- Red/empty carry pools are 6/7 kid takes and 3/1 parent takes; combined
  giggles are 11 takes.

## Decision

Use actual state lifetime instead of predicted carry duration or overlapping
VO players.

## Next acceptance

Lane B adds `Player.organic_reaction_triggered` exactly as specified in
`WIRING.md`; repeated contacts must not retrigger within 2.5 s or add noise.

## Risk

Listening pass still owns cadence. `caught_grunt_02` uses its untouched 0.38 s
runtime copy because A18 found no clean denoise profile window.

## Files

`AudioCasting.gd`, `AudioDirector.gd`, `WIRING.md`.
