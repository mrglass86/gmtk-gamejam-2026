# Lane B handoff — B6 behavior fixpack

## Completed

- `acaed9d` adds `--verify-b6`, which starts `GameClock` and advances real
  SceneTree physics. It observes 9.33 m parent displacement, a 0.68 m kitchen
  approach, 0.00 m dog sleep drift, 3.27 m dog patrol displacement, and
  point-blank suspicion 100.
- Fixed actor navigation by waiting for map synchronization and using exported
  path/target tolerances compatible with the actors' elevated visual origins.
- Dog sleeps for 30 s, then exits the disconnected bed nav island and follows
  its existing patrol. Alert/investigate/bark transitions are unchanged.
- Parent sight gain scales from 1x beyond 4 m to 3x within 2.5 m.

## Current state

B6, clean editor startup, and `--verify-a51` pass. No `.tscn` was edited.

## Decision

Actor acceptance is live-clock SceneTree behavior, not table or class scans.

## Next action

Noah runs CP4/CP5 from the title card and watches the first 35 seconds. Accept
when the parent visibly leaves for the kitchen and the dog stays asleep for
about 30 seconds before a natural-looking bed exit and patrol.

## Risk

The exported dog bed-egress coordinate passes behavior checks but still needs
the director's visual verdict.

## Changed files

`Parent.gd`, `Pet.gd`, shared state/backlog/decisions, this handoff.
