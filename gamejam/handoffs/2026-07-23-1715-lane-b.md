# Lane B handoff — B7 third-run fixpack

## Completed

- `3e2e650` fixes carry arrival against the NavigationAgent's reachable final
  point instead of the blocked crib center. A 20 s timer force-deposits even
  with navigation disabled.
- Deposit now unlocks/detaches the player, then the parent exits to the hall,
  closes BedroomDoor via `Door.close()`, watches it for 3.5 s, peeks toward the
  room, and resumes the live-clock routine.
- Dog bark emits raw 5.0. Door creak behavior is unchanged.
- `--verify-b7` observes a 6.88 s full catch-to-routine cycle, closed door,
  20.09 s forced release, and 35.5 parent suspicion from a dog-bed bark under a
  0.7 test mask.

## Current state

B7, B6, A5.1/B5, A7, audio, and clean startup pass. No `.tscn` was edited.

## Decision

Third-run capture/masking rulings are already pending in `DECISIONS.md`; this
handoff does not duplicate or stage that concurrent edit.

## Next action

Noah replays a catch. Accept when release never sticks, the close/watch/peek
beat reads clearly, and a living-room bark visibly alerts the parent.

## Risk

The directed epilogue timing is mechanically green but still needs camera-scale
readability approval.

## Changed files

`Door.gd`, `Parent.gd`, `Pet.gd`, `Player.gd`, shared status/backlog, handoff.
